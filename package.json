{
  "name": "taplo-pre-commit",
  "version": "0.1.0",
  "description": "pre-commit hook to run Taplo from Node binaries",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Ekstrand <md@ekstrandom.net>",
  "license": "MIT",
  "dependencies": {
    "@taplo/cli": "^0.7.0"
  }
}
